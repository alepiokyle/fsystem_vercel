# TODO - Fix Black Sidebar in Dark Mode

## Steps
- [x] 1. Fix `resources/css/mobile-responsive.css` dark mode override so sidebar stays white
- [x] 2. Add defensive rule in `resources/css/sidebar-shrink.css` to keep sidebar white in all states
- [x] 3. Apply same fixes to `public/resources/css/` copies (the browser actually loads these)
- [x] 4. Verify the fix (build/check)
